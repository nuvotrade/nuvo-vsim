# Irreversible deletion hold — 2026-08-31

No deletion is authorized.

Eighteen Workers are preserved and reversibly isolated with their public production/preview URLs off: `atlas`, `nuvo-proxy`, `nuvo-nmr`, `nuvo-vsim-truth`, `nuvo-vsim-worker`, `vsim-test-worker`, `conductor`, `cors`, `lce`, `nuvo-deals`, `nuvo-docusign`, `nuvo-qvu-manual-research`, `nuvo-wheel-preview`, `nuvo-wheel-autonomy-shadow-preview`, `timdicator-api`, `nuvo-breadcrumb-router`, `nuvo-engine`, and `nuvo-desk-proxy`.

Deletion is held because the observation window has not elapsed, Stage 0 remains 0 of 4 fills, storage needs separate zero-consumer/zero-write proof, and each destructive resource class requires exact Principal authorization.

Additional held candidates:

- public `timdicator` Worker: zero-byte live bundle and no proved Git ancestor;
- public `nuvo-vsim.pages.dev`: legacy surface freshly redeployed by the preservation branch;
- `nuvo-vsim-v5-preview`: no runtime fetch but a legacy link remains;
- router, Telegram, and Wheel islands: dependency and Stage-0 gated.

The next cleanup action is an exact named deletion wave after all applicable gates pass. Worker deletion never implies storage, secret, Access, route, domain, or Pages deletion.
