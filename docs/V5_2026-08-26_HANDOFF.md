# NUVO VSIM V5 consolidated handoff — 2026-08-26

This release remains Authority 2, read-only/propose-only. It has no broker submit, replace, or cancel route. The full suite passes 377/377.

## Underwriting and constitutional controls

- The CSP collateral hurdle is charged inside NEV using the risk-free rate plus the required 4% excess return in `src/underwriter/underwrite.js:54` and `src/underwriter/ev.js:216`.
- CSP ranking and eligibility use NEV per calendar day; CSP ROC/RAROC is removed from the decision surface in `src/underwriter/capital.js:133`, `src/structures/optimizer.js:236`, and `src/underwriter/underwrite.js:185`.
- Short-put concentration uses full assignment notional in `src/portfolio/governor.js:83`. Simultaneous assignment funding uses settled, unborrowed cash in `src/portfolio/governor.js:116` and is enforced in `src/portfolio/governor.js:283`.
- Wheel compatibility records the 1.0 sigma recovery threshold and blocks when at least 40% of assignment paths are stranded in `src/underwriter/underwrite.js:59` and `src/underwriter/underwrite.js:137`.
- Pre-filtered and shortlist-rejected candidates retain reason codes in `src/structures/optimizer.js:163`, `src/structures/optimizer.js:190`, `src/pipeline/cycle.js:127`, and `src/evidence/package.js:129`.
- The three-way operator outcome separates eligible edge, no edge, and no capital in `cloudflare/worker.js:507` and its rendered CSP verdict path at `cloudflare/worker.js:2475`.
- Shadow decision detail retains Governor, assignment-funding, and wheel-compatibility facts in `cloudflare/worker.js:440` and `cloudflare/worker.js:489`.

## Calculator behavior

- Scan and manual calculator modes share the Underwrite tab but retain isolated state in `cloudflare/worker.js:1791` and `cloudflare/worker.js:2041`.
- Covered-call and CSP requests carry independent request stamps and mode checks in the live dashboard script; regression assertions are in `test/production_adapters.test.js:119`.
- Input changes clear stale calculator output before a new result can render; the same test file covers clear-on-change and request isolation.
- The calculator headline can describe a user-specified ticker without requiring that ticker to exist in current custody. Custody remains mandatory only for position-specific lifecycle analysis.
- Covered-call calculations and refusal reasons live in `cloudflare/covered-call-calculator.js`; tests are in `test/covered_call_calculator.test.js`.

## Today's P&L and evidence provenance

- Raw Schwab `currentDayProfitLoss`, `currentDayCost`, `netChange`, and prior-session quantity are preserved during normalization in `cloudflare/schwab-client.js:124`.
- The carried-day-cost identity is applied only for the confirmed carry case in `cloudflare/schwab-client.js:150` and emits `SCHWAB_RECONCILED_CARRIED_CURRENT_DAY_COST` at `cloudflare/schwab-client.js:174`.
- Account-level provenance names the authoritative field and reconciliation convention in `cloudflare/schwab-client.js:889`.
- Full unmodified raw Schwab position rows are retained per account in `cloudflare/schwab-client.js:845` and sealed through `raw_positions_json` at `cloudflare/schwab-client.js:920`; schema migration: `cloudflare/migrations/0013_seal_raw_position_packets.sql:1`.
- The dashboard accepts Today's P&L only when the provenance is `SCHWAB_SUM_RECONCILED_POSITION_DAY_PROFIT_LOSS` in `cloudflare/worker.js:2862`.

## Dashboard consolidation

- Navigation is five questions: Overview, Underwrite, Performance, Decisions, and System. Underwrite replaces Opportunities and contains scan/manual modes; Performance contains the closed-record ledger. Rewrites are in `cloudflare/worker.js:1833`.
- Performance supplies one shared URL-backed filter state for calendar, ticker attribution, strategy attribution, curve range, and ledger rows in `cloudflare/worker.js:1886`, `cloudflare/worker.js:2607`, and `cloudflare/worker.js:2628`.
- Filter state survives reload/back navigation through `popstate` at `cloudflare/worker.js:3182`.

## Phase 1 portfolio instrumentation

- Expiration ladder, deployment, reserve, concentration, and distance-to-strike values are derived from current Schwab custody in `cloudflare/portfolio-report.js:441` through `cloudflare/portfolio-report.js:615`.
- The homepage expiration ladder renders from that data at `cloudflare/worker.js:2281`.
- Cash-reserve and deployed-cap gauges render at `cloudflare/worker.js:2311`.
- Option marks use two decimals; stale quote-derived values are greyed and badged at `cloudflare/worker.js:2347`.
- Open short calls display percent cushion, dollar cushion, and sigma distance at `cloudflare/worker.js:2352`.
- Single-name and expiration caps come from the frozen Constitution (20% and 25%); regression tests are in `test/constitution.test.js:163` and `test/portfolio_report.test.js:201`.

## Realized P&L calendar

- New York `closed_date` is stamped in the shared FIFO performance derivation in `cloudflare/portfolio-report.js`; NYSE full-day closures and early-close trading days are defined starting at `cloudflare/portfolio-report.js:234`.
- Month-scoped calendar aggregation, all-strategies default, and CC+CSP secondary scope begin at `cloudflare/portfolio-report.js:265`.
- The embedded Performance panel, lifetime labels, and calendar controls are in `cloudflare/worker.js:1792`.
- URL keys are `pnlMonth`, `pnlScope`, `pnlTicker`, `pnlStrategy`, `pnlFrom`, and `pnlTo` at `cloudflare/worker.js:1886` and `cloudflare/worker.js:2607`.
- Calendar reconciliation visibly reports RECONCILED or DRIFT and labels cells, ticker, and strategy totals at `cloudflare/worker.js:2683` and `cloudflare/worker.js:2701`.
- Future New York dates are excluded visually and arithmetically. Boundary, holiday, future-state, and shared-filter tests are in `test/performance_calendar.test.js`.

## Release verification

- `npm test`: 377 passed, 0 failed.
- `npx wrangler deploy --dry-run --config cloudflare/wrangler.jsonc`: passed.
- Local bundled `entry.js` matches live Worker version `0139fd54-8237-40ae-8ae9-e27261b936b1` byte for byte; SHA-256 `170547277e33ca1fab967a57c8b16dfe06ae3bc6d9b2dd7dc57b33764be0ab80`.
- D1 and R2 preservation details, checksums, evidence-chain baseline, and rollback are in `docs/V5_2026-08-26_PRESERVATION.md`.
